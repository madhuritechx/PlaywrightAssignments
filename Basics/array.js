//to calculate the total sum of all elements in a JavaScript array using loops and variables.

const numbers = [10, 20, 30, 40, 50];

let sum = 0;
for (let i = 0; i < numbers.length; i++) {
  sum += numbers[i];
}

console.log('Total sum:', sum); 